{
  "name": "{{AGENT_NAME_SLUG}}",
  "version": "1.0.0",
  "description": "{{AGENT_DESCRIPTION}}",
  "main": "api/status.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "fastify": "^5.3.0"
  },
  "engines": {
    "node": ">=18"
  },
  "type": "module"
}
